- name: Test Playbook
  hosts: all
  gather_facts: true
  become_method: sudo
  become_user: root

  tasks:
    - name: Include some tasks
      include: tests/static/playbooks/playbook-included-tasks.yml
      when: test
